apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: painel-umb-route
  namespace: embasa-umb
  labels:
    app: gestao-app
spec:
  to:
    kind: Service
    name: gestao-service
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
